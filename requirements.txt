# Core
python-dotenv>=1.0.0
ftfy>=6.2.0
supabase>=2.4.0
PyJWT[crypto]>=2.8.0

# Document parsing
pypdf>=3.17.0
python-pptx>=0.6.23

# API and server
fastapi>=0.110.0
python-multipart>=0.0.9
uvicorn>=0.29.0
slowapi>=0.1.9

# AI and vector store (pgvector via Supabase — no local chromadb needed)
openai>=1.0.0
anthropic>=0.40.0

# NOTE: dev/test deps (pytest, …) live in requirements-dev.txt and are NOT
# installed in production. chromadb was removed — the live store is pgvector
# (app/vectorstore/pgvector_store.py); the legacy chroma_db module is unused.
